replicaCount: 1

image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: stable

serviceAccount:
  create: true
  name: ""

podAnnotations: {}

podSecurityContext: {}
securityContext: {}

resources: {}
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}

containerPorts:
  - name: http
    containerPort: 80
    protocol: TCP

service:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: http
      name: http
    - port: 443
      targetPort: https
      name: https

ingress:
  enabled: true
  ingressClassName: nginx
  hosts:
    - nginx.local
  tls:
    - secretName: nginx-tls
      hosts:
        - nginx.local
