# pv-prometheus-server.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-prometheus-server
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  hostPath:
    path: "/mnt/data/prometheus-server"